# 增量更新

简单来说，就是只采集网页中**新增的内容**，之前已经采集到内容不会重复采集。

---

如何设置增量更新？

首先，我们要选择一个字段，这个字段能够保证这条记录是`唯一`的。 比如新闻标题、内容页链接。
然后，在右侧设置界面，勾选 `不得重复`。

![incr](img/incr_1.png)

    注意，要确保这个字段是否合适作为 `不得重复`字段，比如时间、动态链接（有时间戳参数）就不适合。

